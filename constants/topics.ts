/**
 * Рубрикатор (тематические категории) — общий список и для специализации
 * наставника (регистрация наставника, шаг 2), и для темы события
 * (попап "Добавить событие"). Значения — ровно те строки, что бэкенд хранит
 * в users.specialization / events.topic и валидирует через IsIn (см.
 * platforma_backend/backend/src/utils/topics.ts) — списки должны совпадать.
 */
export const TOPICS = [
  'Кино',
  'Музыка',
  'Искусство',
  'Литература',
  'Театр',
  'Здоровье',
  'Новые увлечения',
] as const;

export type Topic = (typeof TOPICS)[number];
